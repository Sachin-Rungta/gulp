---
framework: mocha
blacklist:
  - "test/watch.js"
postMerge:
  pattern:
    - "./test/*.js"
  strategy:
    threshold: <5|100s|30m|6h>
    type: <commit|time>
preMerge:
  - "./test/*.js"
preRun:
  - npm i
postRun:
  - node --version
  - node --version
version: 2.0
